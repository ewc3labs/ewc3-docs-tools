---
id: DOCS-45
state: 🟦 coded
title: `--repo` was silently ignored by every command except `migrate-project`
est: S
doc: '[DOCS-45](slices/DOCS-45_repo_was_silently_ignored_by_every_command_except_migrate.md)'
status: ""
source: EWC3_Docs_Tools_Roadmap.md
---

# DOCS-45 — `--repo` was silently ignored by every command except `migrate-project`

> Generated by `ewc3-docs migrate-project` from `EWC3_Docs_Tools_Roadmap.md`. The fields above are that roadmap row, carried across verbatim.
> **The one-line summary has deliberately not been written.** If `title` reads as a
> paragraph rather than a summary, that is the row as it stands — deciding what a slice
> is about, and whether it is finished, is judgement.

`tables --repo <elsewhere>` scanned the CURRENT repo and printed
`Every table row matches its header across 0 file(s)` — a PASS over nothing examined, which is the
exact shape this toolkit exists to catch. The flag value was also left behind as a file spec,
matching nothing
