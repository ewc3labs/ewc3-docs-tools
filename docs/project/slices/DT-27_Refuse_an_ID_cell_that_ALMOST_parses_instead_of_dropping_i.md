---
id: DT-27
state: 🟦 coded
title: Refuse an ID cell that ALMOST parses, instead of dropping it
est: S
doc: '[DT-27](slices/DT-27_Refuse_an_ID_cell_that_ALMOST_parses_instead_of_dropping_i.md)'
status: ""
source: EWC3_Docs_Tools_Roadmap.md
---

# DT-27 — Refuse an ID cell that ALMOST parses, instead of dropping it

> Generated by `ewc3-docs migrate-project` from `EWC3_Docs_Tools_Roadmap.md`. The fields above are that roadmap row, carried across verbatim.
> **The one-line summary has deliberately not been written.** If `title` reads as a
> paragraph rather than a summary, that is the row as it stands — deciding what a slice
> is about, and whether it is finished, is judgement.

`AIR-19 (was VS-19)` in a declaring position makes the ID vanish from every check rather than fail
one; a rename convention that is right in prose silently unmakes the slice in an ID cell ⛔ **LIVE
EVIDENCE 2026-09-03, and the drop is silent exactly as this row predicted.** SX_Coder's Delivery
Index carries 566 authored id rows; `parseIndex` sees **555**. The eleven it cannot read: `VS-09a`
`VS-109a` `VS-197a` `VS-203a` `VS-203b` `VS-203c` `VS-233a` `FIX-22a` (LOWERCASE suffix — the id
regex captures `[A-Z]?`, and this toolkit's own tests use `VS-33A through VS-33D`, so it decided
suffixes were uppercase while the estate writes them lowercase) and `VS-87-R1` `FIX-09-R1`
`FIX-15-R1` (a `-R1` REVISION form nothing models). Cost, measured: `declaredIds` reports `VS-203`
declared and has no entry for `VS-203a/b/c`, so four authored commitments collapse to one and the
collision check cannot see three of them; `migrate-project` emitted ONE document for all four.
`VS-233a` is a live slice cited outside the register. Estate-wide the gap is 11 and **all of it is
SX_Coder** — the other five registers are fully seen. Work: accept lowercase suffixes, and REFUSE an
almost-parsing id cell rather than let it vanish. The `-R1` form is a convention question for the
hub, not a parser question ✅ **GRAMMAR LANDED 2026-09-03.** Suffix ruled by SXC to be two forms,
both DISTINCT IDS: `a`/`b`/`c` sub-slices and `-R<n>` follow-on revisions. The proof they are ids
rather than annotations is that they carry **independent states** — `FIX-09-R1` is smoked while
`FIX-09` is coded, `VS-203c` is planned while `VS-203a`/`b` are coded; a revision-OF would share its
parent's state. Padding applies to the numeric part only and the suffix appends, so `VS-09a` is
canonical at a minimum of two. The 11 are NOT normalised — rewriting live ids breaks every citation
in commits, memories and STATUS entries. ⭐ Fixed by CENTRALISING the grammar rather than correcting
18 regexes: it was spelled out in 18 places across four files in four different forms, which is the
composed-key defect, and it had already drifted. SX_Coder now parses 566 of 566. **Still open:
REFUSE rather than drop** — an id cell that almost parses must be reported, which is the half that
makes the next drift loud instead of silent ⭐ **WHY REFUSE-DON'T-DROP IS WORTH BUILDING, and it is
not the reason I had been giving.** I justified it as protection against other people's malformed
input. SXC's one-off repair script for the four split rows asserted 10 raw cells and the actual
count was 9 — those rows carry no trailing pipe — and its own guard printed *"unexpected raw cell
count 9, skipped"* and merged NOTHING. Written by someone an hour deep in this exact format, and it
caught them on the first run. **The value is that it catches YOUR OWN wrong assumption about the
shape, at the moment you act on it.** ⛔ And the four rows it was repairing show why a quiet refusal
is not enough: **a split-cell defect eats the NEWEST half by construction** — an update appended to
a cell is by definition the most recent thing in it, so a stray separator always hides the latest
state. Those four concealed three Wilson-raised priority changes and a partial-ship note from
collision checking, the round trip AND the canonicality lint, because one character was wrong.
Refusing is correct and INSUFFICIENT; the report must be loud, because the rows it names carry the
newest decisions rather than merely being untidy
