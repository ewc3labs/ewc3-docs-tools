---
id: DOCS-50
state: 🟦 coded
title: STAGED slices must never write the LIVE roadmap, and three other findings that sat unread for six days
est: M
doc: '[DOCS-50](slices/DOCS-50_STAGED_slices_must_never_write_the_LIVE_roadmap_and_three.md)'
status: ""
source: EWC3_Docs_Tools_Roadmap.md
---

# DOCS-50 — STAGED slices must never write the LIVE roadmap, and three other findings that sat unread for six days

> Generated by `ewc3-docs migrate-project` from `EWC3_Docs_Tools_Roadmap.md`. The fields above are that roadmap row, carried across verbatim.
> **The one-line summary has deliberately not been written.** If `title` reads as a
> paragraph rather than a summary, that is the row as it stands — deciding what a slice
> is about, and whether it is finished, is judgement.

⛔ **P1: `index --write` would have rewritten an authoritative register from generated previews.**
The slice directory fell back to `docs/project_v2/slices` while the roadmap target stayed the live
`docs/project` tree — the documented command, in the NORMAL pre-adoption state. **Measured blast
radius: 2 of 6 repos exposed** (docs-tools, HDCTranslators); the other four were saved only because
a README happens to sit in `docs/project/slices`, which is not a design. Rule that removes the
class: **the slices and the roadmap they render into come from the SAME TREE.** Also fixed: two
documents declaring one id are refused rather than silently deduplicated by a Map (`DOCS-30` at the
`index` level — `VS-1_First.md` and `VS-1_Second.md` coexist because the filename carries a slug);
rendering NO Delivery Index is no longer exit 0 (`DOCS-47`); and a row now links to ITS OWN document
rather than the anchor's, which had left every follower's frontmatter unreachable from the index
that exists to project it. 🔴 **THE PROCESS FAILURE MATTERS MORE THAN THE BUGS.** All four were
found by codex on `383bfb7` at 23:48Z on 2026-09-02 and **I never opened the review** — posted the
write-up, got pulled into another thread, and for six days reported the branch *settled* and *clean*
off a stale one. **A completed review is a value that cannot decay, reported as though it were
live** — git will tell you your branch moved; nothing tells you a review arrived that you never
opened. Surfaced by Labs challenging an unrelated claim on a premise that was itself wrong. ⭐ And
three of the four lived in `bin/`, which the suite could not reach: **the most dangerous logic in
the toolkit was also the least testable.** The new regressions drive the real CLI
